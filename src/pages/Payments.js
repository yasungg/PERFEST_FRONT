import React from "react";
import { StyledComponent } from "styled-components";

const Payment = () => {

  const start = () => {
    const toss = async() => {
      
    }
  }
  return(
    <div onClick={start()}>토스로 결제하기</div>
  )
}

export default Payment;